import { Box, Typography } from "@mui/material";

const Dashboard = () => {
  return (
    <Box>
      <Typography variant="h2" fontWeight={600}>
        Welcome
      </Typography>
    </Box>
  );
};

export default Dashboard;
